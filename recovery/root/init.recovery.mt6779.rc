# This kernel exposes ConfigFS rather than /sys/class/android_usb/android0.
# TWRP may request mtp,adb, but this implementation reliably supports the ADB
# path only, so redirect that combination to ADB.
on init
    setprop sys.usb.configfs 1
    setprop sys.usb.ffs.aio_compat 1
    setprop sys.usb.controller musb-hdrc
    setprop service.adb.root 1

on boot
    setprop sys.usb.config adb

on property:sys.usb.config=mtp,adb
    setprop sys.usb.config adb

on fs && property:ro.debuggable=0
    # Select the USB charging/data mode for CDP vs. SDP detection.
    write /sys/class/udc/musb-hdrc/device/cmode 2
    # The ConfigFS sys.usb.config=adb trigger starts adbd and binds musb-hdrc.

on post-fs
    # Support A/B feature for eMMC boot region
    symlink /dev/block/sda /dev/block/mmcblk0boot0
    symlink /dev/block/sdb /dev/block/mmcblk0boot1
    symlink /dev/block/mmcblk0boot0 /dev/block/platform/bootdevice/by-name/preloader_a
    symlink /dev/block/mmcblk0boot1 /dev/block/platform/bootdevice/by-name/preloader_b

    # Support A/B feature for combo emmc and ufs OTA update
    symlink /dev/block/platform/bootdevice/by-name/preloader_a /dev/block/platform/bootdevice/by-name/preloader_emmc_a
    symlink /dev/block/platform/bootdevice/by-name/preloader_b /dev/block/platform/bootdevice/by-name/preloader_emmc_b
    symlink /dev/block/platform/bootdevice/by-name/preloader_a /dev/block/platform/bootdevice/by-name/preloader_ufs_a
    symlink /dev/block/platform/bootdevice/by-name/preloader_b /dev/block/platform/bootdevice/by-name/preloader_ufs_b

service console /system/bin/sh
    class core
    console
    disabled
    seclabel u:r:shell:s0
